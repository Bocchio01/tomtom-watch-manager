{
    "cmake.sourceDirectory": [
        // "${workspaceFolder}",
        "${workspaceFolder}/core",
        "${workspaceFolder}/cli",
        // "${workspaceFolder}/gui",
    ],
    "files.associations": {
        "Doxyfile": "shellscript"
    }
}